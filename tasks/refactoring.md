Рефакторинг кода и анимации — чеклист

1. Инвентаризация типов по проекту — выполнено: найти все объявления `type`/`interface` (напр. `context/ThemeContext.tsx:7–11`, `components/ui/button.tsx:41`, `components/ui/textarea.tsx:5`, `components/ui/tabs.tsx:83`, секции в `components/sections/*`).
2. Подготовка централизованного файла `types/types.ts` — выполнено: единый экспорт (ESM), согласованные имена и группировка (доменные, UI‑props, контексты).
3. Миграция — выполнено: перенести все типы из компонентов/контекстов в `types/types.ts` и обновить импорты по проекту; локальные типы тоже вынести и импортировать обратно (логику/структуру не изменяем).
4. Валидация — выполнено: запустить `tsc --noEmit` и линт, устранить ошибки импортов/циклических зависимостей.
5. Аудит UI (shadcn) — выполнено: собрать список файлов в `components/ui/*`, проверить фактические использования через поиск и зафиксировать кандидатов на удаление.
6. Удаление неиспользуемых компонентов — выполнено: удалить файлы без импортов/ссылок, пересобрать проект и проверить отсутствие ошибок.
7. Финализация: обновить этот чеклист, зафиксировать перенесённые типы и удалённые компоненты; провести короткий код‑ревью.